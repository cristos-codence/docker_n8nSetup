version: '3.8'

services:
  n8n:
    container_name: n8n
    hostname: n8n
    image: n8nio/n8n:stable
    restart: unless-stopped
    environment:
      # USER CONFIGURATION - change these to your own, if needed
      - GENERIC_TIMEZONE=America/New_York

      # Configure Logging
      - N8N_LOG_OUTPUT=console,file
      - N8N_LOG_FILE_COUNT_MAX=10
      - N8N_LOG_FILE_LOCATION=/home/node/.n8n/logs/n8n.log
      - N8N_LOG_LEVEL=info
      - CODE_ENABLE_STDOUT=true

      # Scaffolding for new users
      - N8N_TEMPLATES_ENABLED=true
      - N8N_HIRING_BANNER_ENABLED=false

      # Concurrency and performance management
      - N8N_CONCURRENCY_PRODUCTION_LIMIT=1

      # Executions and database cleanup
      - EXECUTIONS_DATA_PRUNE=true
      - EXECUTIONS_DATA_PRUNE_MAX_COUNT=100
      - EXECUTIONS_DATA_SAVE_ON_PROGRESS=false # Set to false to have fewer executions saved to the database
      - DB_TYPE=sqlite
      - DB_SQLITE_VACUUM_ON_STARTUP=false
      - DB_SQLITE_POOL_SIZE=1

      # Fixes for warnings
      - N8N_RUNNERS_ENABLED=true
      - N8N_ENFORCE_SETTINGS_FILE_PERMISSIONS=true

      # Deployment - only change if you know what you're doing
        # Node settings
      - NODE_ENV=production
        # Network access (can impact tunneling)
      - NODEJS_PREFER_IPV4=true
      - N8N_SECURE_COOKIE=false
      - N8N_PROTOCOL=https
      # Configure for ngrok
      - N8N_HOST=${NGROK_DOMAIN}
      - N8N_EDITOR_BASE_URL=https://${NGROK_DOMAIN}
      - N8N_WEBHOOK_URL=https://${NGROK_DOMAIN}

    volumes:
      - $HOME/Documents/n8n_docker_files:/home/node/.n8n
      - $HOME/Downloads:/data/downloads
    ports:
      - "5678:5678"
    networks:
      - n8n-network
    command: "start"

  ngrok:
    container_name: n8n-ngrok
    image: ngrok/ngrok:latest
    restart: unless-stopped
    environment:
      - NGROK_AUTHTOKEN=${NGROK_AUTHTOKEN}
    command: "http n8n:5678 --domain=${NGROK_DOMAIN}"
    depends_on:
      - n8n
    networks:
      - n8n-network

networks:
  n8n-network:
    driver: bridge
