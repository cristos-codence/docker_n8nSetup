services:
  n8n:
    ports:
      - 5678
    environment:
      - N8N_HOST=n8n-n8n.docker.localhost
      - N8N_PROXY_HOPS=1
    networks:
      - traefik_net

networks:
  traefik_net:
    external: true
